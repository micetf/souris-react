/**
 * Point d'entrée pour les composants communs
 * Exporte tous les composants réutilisables
 */
import Button from "./Button";
import Loading from "./Loading";

// Export des composants communs
export { Button, Loading };

// Par défaut, exporter un objet avec tous les composants
export default {
    Button,
    Loading,
};
